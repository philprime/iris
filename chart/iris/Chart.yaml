apiVersion: v2
name: iris
description: A single public SMTP entrypoint for a Kubernetes cluster, declaratively routed and fanned out to in-cluster services.
type: application
# version is a placeholder for source checkouts. The publish workflow overrides
# both version and appVersion with the release git tag (see make chart-package).
version: 0.0.0
appVersion: "latest"
home: https://github.com/philprime/iris
sources:
  - https://github.com/philprime/iris
keywords:
  - smtp
  - email
  - postfix
  - ingress
  - controller
maintainers:
  - name: Philip Niedertscheider
